{
  "name": "forge-ai-identity",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -Rp \"Forge AI Brand Identity/.\" dist/ && cp \"dist/Brand Catalogue.html\" dist/index.html"
  }
}
