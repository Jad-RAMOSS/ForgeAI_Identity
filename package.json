{
  "name": "forge-ai-identity",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -rp \"Forge AI Brand Identity/.\" dist/ && mv dist/\"Brand Catalogue.html\" dist/index.html"
  }
}
